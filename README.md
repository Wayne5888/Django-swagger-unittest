This is an simple example of api with Django + swagger + unittest + docker.

How to use it?

1. git clone
2. cmd : docker build -t your-django-app .
3. cmd : docker run -p 8000:8000 your-django-app
4. access swagger by http://localhost:8000/swagger/


